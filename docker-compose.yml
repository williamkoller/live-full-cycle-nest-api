version: '3'

services:
  app:
    build: .
    entrypoint: ./.docker/entrypoint.sh
    container_name: live-full-cycle-nest-api
    ports:
      - ${PORT}:${PORT}
    volumes:
      - .:/home/node/app
